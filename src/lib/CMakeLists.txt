# generated by cmake-generator
cmake_minimum_required(VERSION 3.16.3)

add_library(game-relay-serialize STATIC 
  client_in.c
  client_out.c
  debug.c
  serialize.c
  server_in.c
  server_out.c)

include(Tornado.cmake)
set_tornado(game-relay-serialize)

target_include_directories(game-relay-serialize PUBLIC ../include)


target_link_libraries(game-relay-serialize PUBLIC 
  flood)

